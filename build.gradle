group = 'com.orientechnologies'
version = '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile group: 'com.orientechnologies', name: 'orientdb-core', version: '3.0.1-SNAPSHOT'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

task pokecLoad(type: JavaExec, dependsOn: classes) {
    classpath = classpath = sourceSets.main.runtimeClasspath
    main = 'com.orientechnologies.pokec.load.PokecLoad'
    jvmArgs = ["-server"]
}

task pokecRead(type: JavaExec, dependsOn: classes) {
    classpath = classpath = sourceSets.main.runtimeClasspath
    main = 'com.orientechnologies.pokec.read.PokecRead'
    jvmArgs = ["-server"]
}


task pokecContentUpdate(type: JavaExec, dependsOn: classes) {
    classpath = classpath = sourceSets.main.runtimeClasspath
    main = 'com.orientechnologies.pokec.contentupdate.PokecContentUpdate'
    jvmArgs = ["-server"]
}

task pokecEdgesAdd(type: JavaExec, dependsOn: classes) {
    classpath = classpath = sourceSets.main.runtimeClasspath
    main = 'com.orientechnologies.pokec.edgesupdate.PokecEdgesAdd'
    jvmArgs = ["-server"]
}
